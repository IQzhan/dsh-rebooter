/**
 * dsh-rebooter client.
 *
 * The lifecycle menu is the power button on the app window control bar.
 * It posts `/api/dsh-rebooter/action`. This module stays so the plugin
 * loader still has a client entry, and it does not touch the page.
 *
 * @module dsh-rebooter/client
 */

export const inject = []

export function apply() {}
